IIT Secure Suite Activation — Medium RE

Build:
    python3 generate_instances.py

This creates one:
    IITSecureActivator

Every execution generates a new Product ID, and the correct
activation key depends on that Product ID.

Distribute ONLY:
    IITSecureActivator

Do not distribute:
    activation.c
    activation_template.c
    generate_instances.py

Flag:
    IITCTF{4ct1v4t10n_5ucc355}
