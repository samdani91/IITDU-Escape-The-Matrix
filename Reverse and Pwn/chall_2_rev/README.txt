IIT Secure Suite — Medium RE

Build:
    python3 generate_instances.py

Distribute ONLY:
    IITSecureActivator

Every execution generates a new Product ID. The correct activation
key is derived from that Product ID by the binary.

Do not distribute:
    activation.c
    activation_template.c
    generate_instances.py

No CSV and no per-player binaries are required.
